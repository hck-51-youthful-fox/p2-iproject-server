const { Genres } = require(`../models`);

class Controller {
    static async fetchGenres() {
        try {
            
        } catch (error) {
            
        }
    }
}

module.exports = Controller;